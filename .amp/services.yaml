services:
  website:
    command: >-
      export PATH="$HOME/.local/bin:$PATH";
      pnpm preview --host 0.0.0.0
    health: /
    portal:
      title: website
      description: Production preview of the Alexandrite website and playground.
